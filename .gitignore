# Binaries
*.exe
*.dll
*.so
*.dylib
/bin/
/dist/

# Test / coverage
*.test
*.out
coverage.*

# Go workspace
go.work
go.work.sum

# Secrets / local config (NEVER commit ABM private keys)
.env
.env.*
!.env.example
*.pem
*.p8
/secrets/

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store

# Local Claude Code config: ルートの CLAUDE.md は docs/dev/CLAUDE.md への symlink。
# ローカルの自動読み込み用で、配布には含めない（本体は docs/dev/CLAUDE.md）。
/CLAUDE.md
